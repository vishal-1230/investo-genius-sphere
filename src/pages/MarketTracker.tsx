
import React from 'react';
import Layout from '@/components/Layout';
import TrendTracker from '@/components/trends/TrendTracker';

const MarketTracker = () => {
  return (
    <Layout>
      <TrendTracker />
    </Layout>
  );
};

export default MarketTracker;
